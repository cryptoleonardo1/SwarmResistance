import ChatbotPage from '../components/chatbot/ChatbotPage';

// This is just a wrapper component that imports the actual implementation
// Following the same pattern as your HomePage component
const ChatbotPageWrapper = () => {
  return <ChatbotPage />;
};

export default ChatbotPageWrapper;